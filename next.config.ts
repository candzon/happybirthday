import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Matikan indikator dev (badge "N" di pojok) agar screenshot & preview bersih
  devIndicators: false,
};

export default nextConfig;
